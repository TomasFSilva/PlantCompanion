<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlantCompanion.HistoricoPage"
             Title="Histórico de Plantas"
             BackgroundColor="White">

    <StackLayout Padding="10">
        <Label Text="Histórico de Plantas" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />
        <CollectionView x:Name="PlantsCollectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5" BorderColor="LightGray" CornerRadius="10">
                        <StackLayout>
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding HealthStatus}" FontSize="14" />
                            <Image Source="{Binding UserImageUrl}" HeightRequest="100" />
                            <Image Source="{Binding DefaultImageUrl}" HeightRequest="100" />
                            <Button Text="Apagar" Clicked="OnDeleteButtonClicked" CommandParameter="{Binding .}" />
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>